{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "site",
  "tags": ["scope:site", "type:app"],
  "// targets": [
    "build and serve are inferred by the @nx/vite plugin from",
    "vite.config.ts; typecheck and lint are explicit."
  ],
  "targets": {
    "a11y": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "concurrently -k -s first \"http-server apps/site/dist -p 4620 --silent --proxy http://127.0.0.1:4620?\" \"wait-on tcp:127.0.0.1:4620 && pnpm exec playwright test --config apps/site/playwright.config.ts\""
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint apps/site/src --max-warnings 0"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/site/tsconfig.json --noEmit"
      }
    }
  }
}
