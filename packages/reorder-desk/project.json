{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "reorder-desk",
  "tags": ["scope:shared", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/reorder-desk/tsconfig.lib.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run packages/reorder-desk/test --root ."
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/reorder-desk --max-warnings 0"
      }
    }
  }
}
