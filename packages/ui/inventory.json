{
  "note": "Generated from packages/ui/src by scripts/inventory.ts. Every field is copied from something a person wrote — a prop signature, a doc sentence, a story tag — and a component missing one appears with the field absent rather than with a guess. Run `nx run ui:inventory-write` after a deliberate change; `nx run ui:inventory` fails when this file has drifted from the source.",
  "components": [
    {
      "component": "Accordion",
      "props": [
        {
          "name": "items",
          "type": "AccordionItem[]",
          "required": false,
          "doc": "The convenience form."
        },
        {
          "name": "multiple",
          "type": "boolean",
          "required": false,
          "doc": "Allow several open sections at once."
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["Accordion.Item", "Accordion.Trigger", "Accordion.Panel"],
      "slots": [],
      "status": "stable",
      "useFor": "sections where only one answer matters at a time",
      "insteadWhen": "the sections are peer views to navigate between (Tabs)",
      "accessibility": "Base UI renders header triggers with `aria-expanded`/`aria-controls` and labelled regions, and handles the keyboard (Enter/Space, Arrow navigation between triggers)"
    },
    {
      "component": "Alert",
      "props": [
        { "name": "severity", "type": "Severity", "required": true },
        { "name": "title", "type": "ReactNode", "required": false },
        { "name": "children", "type": "ReactNode", "required": true },
        {
          "name": "onDismiss",
          "type": "() => void",
          "required": false,
          "doc": "When provided, a dismiss button renders and calls this."
        },
        { "name": "dismissLabel", "type": "string", "required": false }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "feedback about what the user just did, next to the thing they did it to",
      "insteadWhen": "the message concerns the whole view (Banner)",
      "accessibility": "`warning` and `danger` render `role=\"alert\"` (assertive — something needs attention now); `info` and `success` render `role=\"status\"` (polite). Severity is always repeated in the title/text, never carried by colour alone"
    },
    {
      "component": "Avatar",
      "props": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "doc": "Full name; initials derive from it when no image is given."
        },
        { "name": "src", "type": "string", "required": false },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false
        },
        {
          "name": "decorative",
          "type": "boolean",
          "required": false,
          "doc": "Silence the avatar for assistive technology."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "marking who a row or comment belongs to",
      "insteadWhen": "the image carries meaning of its own (a plain img with alt text)",
      "accessibility": "both branches announce the same thing. The image takes `alt={name}`, the initials take `role=\"img\"` with the same label, so whether someone uploaded a picture does not change what a screen reader says about them. Pass `decorative` where the name is already beside the avatar. The two branches used to disagree — the image silent, the initials announcing — which meant a row's accessibility depended o"
    },
    {
      "component": "AvatarGroup",
      "props": [
        {
          "name": "people",
          "type": "Array<{ name: string; src?: string }>",
          "required": true,
          "doc": "Who is in the group, in the order they should be shown."
        },
        {
          "name": "max",
          "type": "number",
          "required": false,
          "doc": "How many faces to show before the counter."
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false
        },
        {
          "name": "person",
          "type": "(entry: { name: string; src?: string }) => ReactNode",
          "required": false,
          "doc": "Render one face yourself."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What the group is, for assistive technology."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "the handful of people attached to one thing — assignees, reviewers, attendees",
      "insteadWhen": "the reader needs to act on an individual: overlapping faces are not reliable click targets, so use a list",
      "accessibility": "one `role=\"group\"` with `label` as its name, and every avatar inside it `decorative` — so a reader announces the group once and then the names, rather than the word \"avatar\" five times. The names of everyone past `max` are in the counter's own label, so nobody disappears because the layout ran out of room. The overlap is drawn with a ring in the page's own background colour, not a gap, because a g"
    },
    {
      "component": "Badge",
      "props": [
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every style and behaviour."
        },
        {
          "name": "tone",
          "type": "\"accent\" | \"neutral\" | \"danger\" | \"success\"",
          "required": false
        },
        {
          "name": "children",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, not a string."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "labelling a state on a row or card",
      "insteadWhen": "the label performs an action (Button), or the state changes on its own (StatusPill)",
      "accessibility": "a `span` with no role, because a badge is text. Colour is never the only channel — the tone changes the fill and the word inside it still says what it means, so \"12 overdue\" reads the same to someone who cannot see that it is red. A badge that only shows a number needs the noun beside it in the markup, not in the colour"
    },
    {
      "component": "Banner",
      "props": [
        { "name": "severity", "type": "Severity", "required": true },
        { "name": "children", "type": "ReactNode", "required": true }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "an announcement that concerns the whole view",
      "insteadWhen": "the message belongs to one control or row (Alert)",
      "accessibility": "the role follows the severity — `status` for information, `alert` for danger — so a danger banner interrupts and an informational one waits its turn. A banner that appears after page load is announced; one present at load is read in document order"
    },
    {
      "component": "Breadcrumb",
      "props": [
        {
          "name": "items",
          "type": "BreadcrumbItem[]",
          "required": false,
          "doc": "The convenience form, and the one that also decides which crumb is current: the last one."
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "The trail's accessible name."
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["Breadcrumb.Crumb"],
      "slots": [],
      "status": "stable",
      "useFor": "showing where a page sits in a hierarchy the reader can climb",
      "insteadWhen": "the levels are not a hierarchy (a back link)",
      "accessibility": "`nav` with a label, list semantics, separators hidden from assistive technology"
    },
    {
      "component": "Button",
      "props": [
        { "name": "children", "type": "ReactNode", "required": true },
        { "name": "variant", "type": "Variant", "required": false },
        { "name": "tone", "type": "Tone", "required": false },
        { "name": "size", "type": "Size", "required": false },
        {
          "name": "leading",
          "type": "ReactNode",
          "required": false,
          "doc": "Icon/content before the label."
        },
        {
          "name": "trailing",
          "type": "ReactNode",
          "required": false,
          "doc": "Icon/content after the label."
        },
        {
          "name": "loading",
          "type": "boolean",
          "required": false,
          "doc": "Shows an inline spinner, sets `aria-busy` and refuses activation."
        },
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every Button style and behaviour."
        }
      ],
      "parts": [],
      "slots": [
        { "token": "--uix-button-accent-bg", "default": "var(--uix-accent)" },
        {
          "token": "--uix-button-accent-fg",
          "default": "var(--uix-text-on-accent)"
        },
        {
          "token": "--uix-button-ghost-fg",
          "default": "var(--uix-text-primary)"
        },
        {
          "token": "--uix-button-ghost-hover-bg",
          "default": "var(--uix-bg-subtle)"
        },
        {
          "token": "--uix-button-neutral-bg",
          "default": "var(--uix-surface-inverse)"
        },
        {
          "token": "--uix-button-neutral-fg",
          "default": "var(--uix-text-on-inverse)"
        },
        {
          "token": "--uix-button-outline-bg",
          "default": "var(--uix-bg-surface)"
        },
        {
          "token": "--uix-button-outline-border",
          "default": "var(--uix-border-subtle)"
        },
        {
          "token": "--uix-button-outline-border-strong",
          "default": "var(--uix-border-strong)"
        },
        {
          "token": "--uix-button-outline-fg",
          "default": "var(--uix-text-primary)"
        },
        {
          "token": "--uix-button-radius",
          "default": "var(--uix-radius-control)"
        }
      ],
      "status": "stable",
      "useFor": "anything that performs an action",
      "insteadWhen": "the control navigates: pass `renderAs={<a href=\"…\" />}` so it stays a link",
      "accessibility": "a native `<button>` by default; the focus ring is `focus-visible` only. `loading` sets `aria-busy` and `aria-disabled` rather than the native `disabled`, because a disabled button leaves the tab order — a user tabbing a form would lose their place the moment a button started loading. ### Theming Override slots for this component. None is declared: each is referenced with its semantic default inlin"
    },
    {
      "component": "Card",
      "props": [],
      "parts": ["Card.Media", "Card.Header", "Card.Body", "Card.Footer"],
      "slots": [{ "token": "--uix-card-media-ratio", "default": "16 / 9" }],
      "passthrough": true,
      "status": "stable",
      "useFor": "a summary that links somewhere else",
      "insteadWhen": "the surface is a section of this page (Panel)",
      "accessibility": "`article` with an optional labelled header. Every slot forwards its attributes, so `aria-label`, `id` and `role` can be set where they belong instead of being unreachable. Theming: | Token | Default | Meaning | | --- | --- | --- | | `--uix-card-media-ratio` | `16 / 9` | Box a card's banner image is cropped to | The ratio is a slot as well as a prop because a brand usually wants one answer for ever"
    },
    {
      "component": "Checkbox",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node."
        },
        { "name": "checked", "type": "boolean", "required": false },
        { "name": "defaultChecked", "type": "boolean", "required": false },
        {
          "name": "onCheckedChange",
          "type": "(checked: boolean) => void",
          "required": false,
          "doc": "The checked triple, matching value/defaultValue/onValueChange elsewhere: checked, defaultChecked, onCheckedChange."
        },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "indeterminate",
          "type": "boolean",
          "required": false,
          "doc": "Tri-state support: aria-checked=\"mixed\" plus a dash."
        },
        {
          "name": "hint",
          "type": "ReactNode",
          "required": false,
          "doc": "Instruction under the control, linked with aria-describedby."
        },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the control required, visibly and for assistive technology."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "independent options, each on or off",
      "insteadWhen": "the options are mutually exclusive (RadioGroup), or the change applies immediately (Switch)",
      "accessibility": "Base UI renders role/aria-checked (including \"mixed\"), keeps the root focusable and handles Space"
    },
    {
      "component": "Chip",
      "props": [
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every style and behaviour."
        },
        { "name": "children", "type": "ReactNode", "required": true },
        {
          "name": "interactive",
          "type": "boolean",
          "required": false,
          "doc": "Static chips are plain labels (a card's tag list)."
        },
        { "name": "active", "type": "boolean", "required": false },
        {
          "name": "defaultActive",
          "type": "boolean",
          "required": false,
          "doc": "The uncontrolled half; a filter chip that owns its own state."
        },
        {
          "name": "onActiveChange",
          "type": "(active: boolean) => void",
          "required": false,
          "doc": "Was `onSelect`, which said nothing about what changed."
        },
        {
          "name": "disabled",
          "type": "boolean",
          "required": false,
          "doc": "Unavailable."
        }
      ],
      "parts": [],
      "slots": [
        {
          "token": "--uix-chip-active-bg",
          "default": "var(--uix-surface-inverse)"
        },
        {
          "token": "--uix-chip-active-fg",
          "default": "var(--uix-text-on-inverse)"
        },
        { "token": "--uix-chip-bg", "default": "var(--uix-bg-subtle)" },
        { "token": "--uix-chip-fg", "default": "var(--uix-text-secondary)" }
      ],
      "status": "stable",
      "useFor": "a compact tag, or a filter that toggles",
      "insteadWhen": "the choice is one of a few fixed views (SegmentedControl)",
      "accessibility": "a static chip is a `span` and says nothing on its own; a filter chip is a native `button` with `aria-pressed`, so its state reaches assistive technology without a hidden checkbox. It is a toggle rather than a link because activating it changes this page"
    },
    {
      "component": "Combobox",
      "props": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name."
        },
        { "name": "label", "type": "ReactNode", "required": true },
        { "name": "hint", "type": "ReactNode", "required": false },
        { "name": "error", "type": "ReactNode", "required": false },
        { "name": "required", "type": "boolean", "required": false },
        { "name": "hideLabel", "type": "boolean", "required": false },
        {
          "name": "options",
          "type": "Array<ComboboxOption | string>",
          "required": false,
          "doc": "The options, as strings or as descriptors."
        },
        {
          "name": "value",
          "type": "string | string[] | null",
          "required": false,
          "doc": "The chosen value, or values when `multiple` is set."
        },
        {
          "name": "defaultValue",
          "type": "string | string[] | null",
          "required": false
        },
        {
          "name": "onValueChange",
          "type": "(next: string | string[] | null) => void",
          "required": false
        },
        {
          "name": "multiple",
          "type": "boolean",
          "required": false,
          "doc": "Allow more than one."
        },
        {
          "name": "onQueryChange",
          "type": "(query: string) => void",
          "required": false,
          "doc": "Called as the reader types, for a list that comes from a server."
        },
        {
          "name": "loading",
          "type": "boolean",
          "required": false,
          "doc": "Results are on their way."
        },
        {
          "name": "filter",
          "type": "(option: ComboboxOption, query: string) => boolean",
          "required": false,
          "doc": "Which options match what has been typed."
        },
        {
          "name": "option",
          "type": "(entry: ComboboxOption, state: { selected: boolean }) => ReactNode",
          "required": false,
          "doc": "Render one option yourself."
        },
        { "name": "placeholder", "type": "string", "required": false },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "empty",
          "type": "ReactNode",
          "required": false,
          "doc": "Shown when nothing matches."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "choosing from a list too long to scan, where the list may come from a server, hold more than one answer, or need rows that are not just text",
      "insteadWhen": "the list is short and fixed: that is `Select`, and it costs a fraction of this and comes with the operating system's own picker",
      "accessibility": "a `combobox` with `aria-expanded` and `aria-controls` over a `listbox` of `option`s, and **focus stays in the text field** while `aria-activedescendant` reports the highlighted row. That is the same pattern as `CommandPalette`, on purpose — the two are the same shape of problem, and a library where two components solve it differently is a library where one of them is wrong. The keyboard: ArrowDown"
    },
    {
      "component": "CommandPalette",
      "props": [
        { "name": "commands", "type": "Command[]", "required": true },
        { "name": "open", "type": "boolean", "required": false },
        { "name": "defaultOpen", "type": "boolean", "required": false },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "onSelect",
          "type": "(command: Command) => void",
          "required": false
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What this palette searches."
        },
        { "name": "placeholder", "type": "string", "required": false },
        {
          "name": "empty",
          "type": "ReactNode",
          "required": false,
          "doc": "Shown when nothing matches."
        },
        {
          "name": "filter",
          "type": "(command: Command, query: string) => boolean",
          "required": false,
          "doc": "Match a command against what has been typed."
        },
        {
          "name": "item",
          "type": "(command: Command) => ReactNode",
          "required": false,
          "doc": "Render one row yourself."
        },
        {
          "name": "hints",
          "type": "ReactNode",
          "required": false,
          "doc": "The key legend along the bottom."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "reaching any action or place in an application by typing",
      "insteadWhen": "the set is small and stable: that is a `Menu`, and a palette over six commands is a search box in front of a list nobody needed to search",
      "accessibility": "a `combobox` with `aria-expanded` and `aria-controls`, over a `listbox` of `option`s, inside a modal dialog. Focus **stays in the text field** the whole time and the highlighted row is reported with `aria-activedescendant` — which is the whole reason this is not the tree or toolbar pattern. Moving DOM focus onto each row as the arrows walk it would take focus out of the input, and then typing anot"
    },
    {
      "component": "DataTable",
      "props": [
        {
          "name": "caption",
          "type": "ReactNode",
          "required": true,
          "doc": "The table's accessible name."
        },
        {
          "name": "columns",
          "type": "Array<DataColumn<Row>>",
          "required": true
        },
        { "name": "rows", "type": "Row[]", "required": true },
        {
          "name": "rowKey",
          "type": "(row: Row) => string",
          "required": true,
          "doc": "A stable identity per row."
        },
        {
          "name": "rowLabel",
          "type": "(row: Row) => string",
          "required": false,
          "doc": "What a row is called, for the accessible name of its selection checkbox."
        },
        { "name": "sort", "type": "DataTableSort | null", "required": false },
        {
          "name": "defaultSort",
          "type": "DataTableSort | null",
          "required": false
        },
        {
          "name": "onSortChange",
          "type": "(next: DataTableSort | null) => void",
          "required": false
        },
        {
          "name": "selectable",
          "type": "boolean",
          "required": false,
          "doc": "Turn on the selection column."
        },
        { "name": "selected", "type": "string[]", "required": false },
        { "name": "defaultSelected", "type": "string[]", "required": false },
        {
          "name": "onSelectedChange",
          "type": "(next: string[]) => void",
          "required": false
        },
        {
          "name": "stickyHeader",
          "type": "boolean",
          "required": false,
          "doc": "Keep the header visible while the body scrolls."
        },
        {
          "name": "rowHeight",
          "type": "number",
          "required": false,
          "doc": "Row height in pixels."
        },
        {
          "name": "height",
          "type": "number",
          "required": false,
          "doc": "The scroll viewport's height."
        },
        {
          "name": "empty",
          "type": "ReactNode",
          "required": false,
          "doc": "Shown instead of the body when there are no rows."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a set of records compared across the same columns, where the reader sorts, selects or scrolls through more of them than fit",
      "insteadWhen": "the markup is the point and there is nothing to sort — `Table` styles a table you wrote and stays out of the way",
      "accessibility": "the header cells carry `aria-sort`, which is how a screen reader says a column is ordered and in which direction — the arrow is for the eye and announces nothing. Each sortable header is a real `<button>` inside its `<th>`, so it is reachable and operable with the keys the platform already gives a button. The scroll viewport is focusable with a name, because `overflow` makes a region only a pointe"
    },
    {
      "component": "DatePicker",
      "props": [
        { "name": "label", "type": "ReactNode", "required": true },
        { "name": "hint", "type": "ReactNode", "required": false },
        { "name": "error", "type": "ReactNode", "required": false },
        { "name": "required", "type": "boolean", "required": false },
        { "name": "hideLabel", "type": "boolean", "required": false },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name, so a `Form` can route an error here."
        },
        {
          "name": "value",
          "type": "IsoDate | [IsoDate, IsoDate | null] | null",
          "required": false,
          "doc": "The chosen date, or the two ends when `range` is set."
        },
        {
          "name": "defaultValue",
          "type": "IsoDate | [IsoDate, IsoDate | null] | null",
          "required": false
        },
        {
          "name": "onValueChange",
          "type": "(next: IsoDate | [IsoDate, IsoDate | null] | null) => void",
          "required": false
        },
        {
          "name": "range",
          "type": "boolean",
          "required": false,
          "doc": "Pick two ends rather than one day."
        },
        {
          "name": "locale",
          "type": "string",
          "required": false,
          "doc": "Which locale the calendar is written in."
        },
        {
          "name": "min",
          "type": "IsoDate",
          "required": false,
          "doc": "Nothing before this date can be picked."
        },
        {
          "name": "max",
          "type": "IsoDate",
          "required": false,
          "doc": "Nothing after this date can be picked."
        },
        {
          "name": "disabledDate",
          "type": "(date: IsoDate) => boolean",
          "required": false,
          "doc": "Refuse individual dates — holidays, days already booked."
        },
        { "name": "disabled", "type": "boolean", "required": false },
        { "name": "placeholder", "type": "string", "required": false },
        {
          "name": "open",
          "type": "boolean",
          "required": false,
          "doc": "Whether the calendar is showing."
        },
        { "name": "defaultOpen", "type": "boolean", "required": false },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "day",
          "type": "( date: IsoDate, state: { selected: boolean; outside: boolean; refused: boolean }, ) => ReactNode",
          "required": false,
          "doc": "Render one day's cell yourself."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "picking a day, or two ends of a span",
      "insteadWhen": "the reader knows the date and there is nothing to browse: a `TextField` with a pattern is faster to type into and far cheaper, and a calendar adds nothing to \"enter your date of birth\"",
      "accessibility": "**a text input with a calendar beside it**, not a calendar alone. Typing is faster than clicking for anybody who already knows the date, and for many people using assistive technology it is the only practical route — a month grid is thirty-five stops to reach one day. The input is the control; the calendar is the enhancement, the same way `FileUpload`'s drop zone sits over a real file input. The g"
    },
    {
      "component": "Dialog",
      "props": [
        {
          "name": "open",
          "type": "boolean",
          "required": false,
          "doc": "Controlled."
        },
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "doc": "The uncontrolled half of the triple, which was missing."
        },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "modal",
          "type": "boolean | \"trap-focus\"",
          "required": false,
          "doc": "Whether the page behind is inert while this is open."
        },
        { "name": "title", "type": "string", "required": true },
        { "name": "description", "type": "string", "required": false },
        { "name": "children", "type": "ReactNode", "required": false },
        {
          "name": "footer",
          "type": "ReactNode",
          "required": false,
          "doc": "Footer slot, typically action buttons."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "a task that must be finished or abandoned before anything else",
      "insteadWhen": "the content is supplementary (Popover), or it is a yes/no decision (AlertDialog)",
      "accessibility": "Base UI restores focus to the trigger on close, wires `aria-labelledby`/`aria-describedby`, and closes on Escape or a backdrop click. The modal half is ours. Measured against `1.0.0-rc.0` the popup had `role=\"dialog\"` and nothing else: no `aria-modal`, nothing inert behind it, no focus trap — so a keyboard user could tab out of the dialog into the page it was covering and operate it. Passing Base "
    },
    {
      "component": "Divider",
      "props": [
        {
          "name": "orientation",
          "type": "\"horizontal\" | \"vertical\"",
          "required": false
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "Accessible name when the divider groups content semantically."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "separating groups that already read as groups",
      "insteadWhen": "the separation needs a name (a heading)",
      "accessibility": "renders `role=\"separator\"`; purely decorative by default — pass a `label` only when the divider means something (e.g. between two form sections a screen reader user navigates)"
    },
    {
      "component": "Drawer",
      "props": [
        { "name": "open", "type": "boolean", "required": false },
        { "name": "defaultOpen", "type": "boolean", "required": false },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "side",
          "type": "\"inline-start\" | \"inline-end\" | \"block-end\"",
          "required": false,
          "doc": "Which edge it comes from."
        },
        {
          "name": "modal",
          "type": "boolean | \"trap-focus\"",
          "required": false,
          "doc": "Whether the page behind is inert while this is open."
        },
        { "name": "title", "type": "string", "required": true },
        { "name": "description", "type": "string", "required": false },
        { "name": "children", "type": "ReactNode", "required": false },
        {
          "name": "footer",
          "type": "ReactNode",
          "required": false,
          "doc": "Footer slot, typically action buttons."
        },
        {
          "name": "size",
          "type": "string",
          "required": false,
          "doc": "How wide, or how tall for `block-end`."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a panel of detail or controls beside the thing it belongs to — filters for a list, the record behind a row, a form that keeps its context visible",
      "insteadWhen": "the task must be finished or abandoned before anything else: that is `Dialog`, and it should cover the page rather than sit beside it",
      "accessibility": "the modal half is this library's rather than Base UI's, for the reason recorded on `Dialog` — measured against Base UI `1.0.0-rc.0` the popup had `role=\"dialog\"` and nothing else, so `aria-modal` and `inert` on every sibling branch are wired here and `browser/focus.spec.ts` holds them down. `modal` matters more here than on a dialog, because a drawer has a real non-modal use. A filter panel beside"
    },
    {
      "component": "EmptyState",
      "props": [
        {
          "name": "title",
          "type": "ReactNode",
          "required": true,
          "doc": "What is not here, as a heading."
        },
        {
          "name": "description",
          "type": "ReactNode",
          "required": false,
          "doc": "Why, or what to do about it."
        },
        {
          "name": "action",
          "type": "ReactNode",
          "required": false,
          "doc": "The one thing to do next."
        },
        {
          "name": "illustration",
          "type": "ReactNode",
          "required": false,
          "doc": "A picture, hidden from assistive technology."
        },
        {
          "name": "headingLevel",
          "type": "2 | 3 | 4",
          "required": false,
          "doc": "The heading level, when this sits inside a page that has headings."
        },
        {
          "name": "children",
          "type": "ReactNode",
          "required": false,
          "doc": "Anything else, below the action."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "the place where a list, table or search result would be, when there is nothing to show",
      "insteadWhen": "the emptiness is an error or a failure — `Alert` says something went wrong, this says nothing is here yet",
      "accessibility": "a `role=\"status\"` region, polite. Status rather than plain markup because this text usually appears as the *result of something the reader did* — a search, a filter — and a result that renders silently leaves a screen reader user waiting. Deliberately **without** a name of its own. An `aria-labelledby` pointing at the title was the first attempt and it was wrong: a live region is announced by its "
    },
    {
      "component": "Field",
      "props": [
        { "name": "label", "type": "ReactNode", "required": true },
        {
          "name": "hint",
          "type": "ReactNode",
          "required": false,
          "doc": "Instruction, shown before the error."
        },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and programmatically."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        {
          "name": "aside",
          "type": "ReactNode",
          "required": false,
          "doc": "Content at the end of the label row: a live value, a character count, the word \"optional\"."
        },
        {
          "name": "nameBy",
          "type": "\"for\" | \"aria\"",
          "required": false,
          "doc": "How the control gets its accessible name."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The control's form field name."
        },
        {
          "name": "children",
          "type": "(props: FieldRenderProps) => ReactNode",
          "required": true
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "building a control that needs a label, a hint, an error and the aria wiring between them — a date picker, a currency input, an uploader, anything not already in this library",
      "insteadWhen": "the control exists here already: TextField, Select, Combobox, NumberField and Slider are all Field underneath, and using them means the wiring is already right",
      "accessibility": "this owns the four things a hand-rolled field forgets — a label bound by `htmlFor` to a real `id`, `aria-describedby` covering whichever of hint and error exist, `aria-invalid` whenever there is an error, and the required state as a word rather than only an asterisk. What the caller still owes: putting the id on the control it renders, and writing an error message that says what to do rather than "
    },
    {
      "component": "FileUpload",
      "props": [
        { "name": "label", "type": "ReactNode", "required": true },
        { "name": "hint", "type": "ReactNode", "required": false },
        { "name": "error", "type": "ReactNode", "required": false },
        { "name": "required", "type": "boolean", "required": false },
        { "name": "hideLabel", "type": "boolean", "required": false },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name, so a `Form` can route an error here."
        },
        {
          "name": "accept",
          "type": "string",
          "required": false,
          "doc": "What the file picker accepts, in the `accept` attribute's own syntax."
        },
        { "name": "multiple", "type": "boolean", "required": false },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "items",
          "type": "UploadItem[]",
          "required": false,
          "doc": "The files chosen so far, with their progress."
        },
        {
          "name": "onFilesSelected",
          "type": "(files: File[]) => void",
          "required": false
        },
        {
          "name": "onRemove",
          "type": "(id: string) => void",
          "required": false
        },
        {
          "name": "item",
          "type": "(entry: UploadItem) => ReactNode",
          "required": false,
          "doc": "Render one row of the list yourself."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "choosing files to send somewhere, with the result of each one visible",
      "insteadWhen": "nothing is uploaded — a plain `<input type=\"file\">` inside a `Field` is the right amount of machinery for a form that submits its files with everything else",
      "accessibility": "the drop zone is a **label wrapping a real `<input type=\"file\">`**, not a div with a drop handler. That is the whole design. A div can be dropped on and cannot be reached, focused, or activated from a keyboard, and there is no way to add those three back that a browser has not already done better. Drag and drop is the enhancement; the input is the control. Progress is a `<progress>` element with i"
    },
    {
      "component": "Form",
      "props": [
        {
          "name": "errors",
          "type": "Record<string, ReactNode>",
          "required": false,
          "doc": "Errors by field name — the shape a server returns them in."
        },
        {
          "name": "busy",
          "type": "boolean",
          "required": false,
          "doc": "Submitting."
        },
        {
          "name": "summaryOn",
          "type": "\"submit\" | \"always\"",
          "required": false,
          "doc": "When the summary appears and takes focus."
        },
        {
          "name": "onSubmit",
          "type": "(event: FormEvent<HTMLFormElement>) => void",
          "required": false
        },
        { "name": "children", "type": "ReactNode", "required": true }
      ],
      "parts": ["Form.Summary", "Form.Actions", "Form.Group"],
      "slots": [],
      "status": "beta",
      "useFor": "a set of fields submitted together, where validation comes back per field",
      "insteadWhen": "there is one control and nothing to summarise — a search box is not a form",
      "accessibility": "the summary is a `role=\"alert\"` region with a heading and one link per failed field, each moving **focus** to the control rather than only scrolling to it — the difference a keyboard user feels. On a submit that produced errors, focus moves to the summary: WCAG 3.3.1 asks for identification and 3.3.3 for a suggestion, and a summary nobody is sent to satisfies neither. What the caller still owes is"
    },
    {
      "component": "IconButton",
      "props": [
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every style and behaviour."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "The accessible name."
        },
        { "name": "children", "type": "ReactNode", "required": true },
        { "name": "variant", "type": "Variant", "required": false },
        { "name": "size", "type": "Size", "required": false }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "an action whose icon is unambiguous, where space is tight",
      "insteadWhen": "the action is primary, or the icon needs explaining (Button with a leading icon)",
      "accessibility": "same native button as Button, but the accessible name is a required prop — TypeScript rejects an icon without one, so the failure mode never reaches production"
    },
    {
      "component": "InlineEdit",
      "props": [
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What is being edited."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "doc": "Controlled."
        },
        {
          "name": "defaultValue",
          "type": "string",
          "required": false,
          "doc": "The uncontrolled half of the triple."
        },
        {
          "name": "onValueChange",
          "type": "(next: string) => void",
          "required": false
        },
        {
          "name": "placeholder",
          "type": "string",
          "required": false,
          "doc": "Shown in the reading state when the value is empty."
        },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "display",
          "type": "(value: string) => ReactNode",
          "required": false,
          "doc": "Render the reading state yourself."
        },
        {
          "name": "validate",
          "type": "(next: string) => string | undefined",
          "required": false,
          "doc": "Validate before leaving edit mode."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "one value in a dense layout that is usually read and occasionally changed — a name in a table row, a title above a page",
      "insteadWhen": "several values change together: that is a `Form`, and a row of independent inline edits saves each one separately with no way to cancel the set",
      "accessibility": "the reading state is a **button**, not a div with a click handler and not a text field styled to look flat. A button says \"this does something\" to a screen reader and is reachable with Tab; a flat input says \"type here\" and then swallows the arrow keys of anyone navigating past it. The switch to editing is announced through a `role=\"status\"`, because replacing a button with a text field is a chang"
    },
    {
      "component": "Menu",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": false,
          "doc": "The trigger's content."
        },
        {
          "name": "items",
          "type": "MenuItemDescriptor[]",
          "required": false,
          "doc": "The convenience form: a list of items."
        },
        { "name": "children", "type": "ReactNode", "required": false },
        {
          "name": "open",
          "type": "boolean",
          "required": false,
          "doc": "Whether the popup is open."
        },
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "doc": "Open on mount, then uncontrolled."
        },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "side",
          "type": "\"top\" | \"bottom\" | \"left\" | \"right\"",
          "required": false,
          "doc": "Which side of the trigger the popup prefers."
        },
        {
          "name": "align",
          "type": "\"start\" | \"center\" | \"end\"",
          "required": false,
          "doc": "How the popup lines up with the trigger along that side."
        }
      ],
      "parts": ["Menu.Item", "Menu.Separator", "Menu.Group", "Menu.GroupLabel"],
      "slots": [],
      "status": "stable",
      "useFor": "a list of actions triggered from one control",
      "insteadWhen": "the items pick a value rather than act (Select)",
      "accessibility": "Base UI renders the full menu pattern — trigger with `aria-haspopup`/`aria-expanded`, roving focus, Arrow/Home/End/ Escape, and typeahead. Items announce `menuitem` within a labelled menu"
    },
    {
      "component": "NumberField",
      "props": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name."
        },
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, now that a real `<label>` carries the accessible name."
        },
        { "name": "hint", "type": "ReactNode", "required": false },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and for assistive technology."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        { "name": "min", "type": "number", "required": false },
        { "name": "max", "type": "number", "required": false },
        { "name": "step", "type": "number", "required": false },
        { "name": "defaultValue", "type": "number", "required": false },
        { "name": "value", "type": "number", "required": false },
        {
          "name": "onValueChange",
          "type": "(value: number | null) => void",
          "required": false
        },
        {
          "name": "disabled",
          "type": "boolean",
          "required": false,
          "doc": "Declared here rather than inherited from the wrapper div, because `rest` lands on the wrapper and a `disabled` that never reaches the control is a field that looks unavailable and still accepts input."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a quantity where stepping by one is a normal move",
      "insteadWhen": "any text is valid (TextField)",
      "accessibility": "Base UI renders a labelled-capable input with `aria-valuenow`-style announcements, and the steppers are real buttons with names (\"Decrease\"/\"Increase\" from the locale)"
    },
    {
      "component": "Pagination",
      "props": [
        { "name": "pageCount", "type": "number", "required": true },
        {
          "name": "page",
          "type": "number",
          "required": false,
          "doc": "The controlled page."
        },
        { "name": "defaultPage", "type": "number", "required": false },
        {
          "name": "onPageChange",
          "type": "(page: number) => void",
          "required": false
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "The landmark's accessible name, overriding the locale default."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "moving through pages a reader may want to return to",
      "insteadWhen": "the list is an endless feed (load more)",
      "accessibility": "one `nav` labelled \"Pagination\"; the current page is a button with `aria-current=\"page\"`; prev/next carry accessible names that survive translation because they are plain text here"
    },
    {
      "component": "Panel",
      "props": [
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every style and behaviour."
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "Uppercase kicker; also the section's accessible name."
        },
        {
          "name": "children",
          "type": "ReactNode",
          "required": true,
          "doc": "Compose richer headers with <PanelHeader>/<PanelBody>."
        }
      ],
      "parts": ["Panel.Header", "Panel.Body"],
      "slots": [
        { "token": "--uix-panel-accent", "default": "var(--uix-accent)" },
        { "token": "--uix-panel-bg", "default": "var(--uix-bg-surface)" },
        {
          "token": "--uix-panel-border",
          "default": "var(--uix-border-subtle)"
        },
        { "token": "--uix-panel-pad-x", "default": "var(--uix-gap-xl)" },
        {
          "token": "--uix-panel-pad-y",
          "default": "calc(var(--uix-gap-lg) + var(--uix-gap-xs))"
        },
        {
          "token": "--uix-panel-radius",
          "default": "var(--uix-radius-container)"
        }
      ],
      "status": "stable",
      "useFor": "a titled region of this page",
      "insteadWhen": "the surface summarises something that lives elsewhere (Card)",
      "accessibility": "`label` names the section (aria-label). For richer content, compose PanelHeader/PanelBody and label the header heading instead — pass `labelledBy` then. Slots: `Panel.Header` and `Panel.Body` map to styled subcomponents, the way enterprise systems expose anatomy without extra props. ### Theming Override slots for this component. None is declared: each is referenced with its semantic default inline"
    },
    {
      "component": "Popover",
      "props": [
        {
          "name": "trigger",
          "type": "ReactNode",
          "required": true,
          "doc": "The trigger's content."
        },
        { "name": "title", "type": "ReactNode", "required": false },
        { "name": "children", "type": "ReactNode", "required": true },
        {
          "name": "open",
          "type": "boolean",
          "required": false,
          "doc": "Whether the popup is open."
        },
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "doc": "Open on mount, then uncontrolled."
        },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        },
        {
          "name": "side",
          "type": "\"top\" | \"bottom\" | \"left\" | \"right\"",
          "required": false,
          "doc": "Which side of the trigger the popup prefers."
        },
        {
          "name": "align",
          "type": "\"start\" | \"center\" | \"end\"",
          "required": false,
          "doc": "How the popup lines up with the trigger along that side."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "detail or a small form without blocking the page",
      "insteadWhen": "the task must be completed first (Dialog)",
      "accessibility": "Base UI wires the trigger's `aria-haspopup` and `aria-expanded`, moves focus into the popup, traps it while open, closes on Escape and returns focus to the trigger. The popup is labelled by its title when there is one. It is non-modal: the page behind stays readable, which is the difference from Dialog"
    },
    {
      "component": "ProgressBar",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node: it renders into a real label element, not an attribute."
        },
        {
          "name": "value",
          "type": "number",
          "required": false,
          "doc": "Omit for an indeterminate bar."
        },
        { "name": "max", "type": "number", "required": false }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "progress with a known proportion, or a named indeterminate wait",
      "insteadWhen": "the wait is short and local (Spinner)",
      "accessibility": "Base UI renders `role=\"progressbar\"` with `aria-valuenow/min/max`; in the indeterminate mode it drops the numbers (they would be noise) — the label is what gets announced"
    },
    {
      "component": "RadioGroup",
      "props": [
        { "name": "name", "type": "string", "required": true },
        {
          "name": "legend",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, because a group's legend can carry a hint or a link."
        },
        {
          "name": "options",
          "type": "RadioOption[]",
          "required": false,
          "doc": "The convenience form."
        },
        { "name": "defaultValue", "type": "string", "required": false },
        { "name": "value", "type": "string", "required": false },
        {
          "name": "onValueChange",
          "type": "(value: string) => void",
          "required": false,
          "doc": "The value triple: value, defaultValue, onValueChange."
        },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "hint",
          "type": "ReactNode",
          "required": false,
          "doc": "Instruction under the group, linked with aria-describedby."
        },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the group required, visibly and for assistive technology."
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["RadioGroup.Option"],
      "slots": [],
      "status": "stable",
      "useFor": "one choice from a small set, all of it visible",
      "insteadWhen": "there are more than about seven options (Select)",
      "accessibility": "a real `fieldset` with a real `legend`, so the group's name is announced once and each option inherits it. The platform gives arrow-key navigation and the single-tab-stop behaviour for free, which is why this is not a set of buttons with `aria-checked`"
    },
    {
      "component": "SearchInput",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "The field's accessible name, required."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        {
          "name": "showLabel",
          "type": "boolean",
          "required": false,
          "doc": "@deprecated Use `hideLabel` — the inverse — for consistency with every other field."
        },
        { "name": "hint", "type": "ReactNode", "required": false },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and for assistive technology."
        }
      ],
      "parts": [],
      "slots": [
        { "token": "--uix-search-radius", "default": "var(--uix-radius-pill)" }
      ],
      "status": "stable",
      "useFor": "filtering or querying a collection",
      "insteadWhen": "it is an ordinary labelled field (TextField)",
      "accessibility": "an accessible name is required, not optional. `label` renders a real `label` element, visually hidden by default because a search field beside a magnifier rarely wants a visible one — hidden is not absent. Before this the component took no label at all, so an unnamed search box was the default and every caller had to remember `aria-label`"
    },
    {
      "component": "SegmentedControl",
      "props": [
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "The group's accessible name."
        },
        {
          "name": "options",
          "type": "SegmentedOption[]",
          "required": false,
          "doc": "The convenience form."
        },
        { "name": "value", "type": "string", "required": false },
        {
          "name": "defaultValue",
          "type": "string",
          "required": false,
          "doc": "The uncontrolled half of the triple, which was simply missing."
        },
        {
          "name": "onValueChange",
          "type": "(value: string) => void",
          "required": false
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["SegmentedControl.Option"],
      "slots": [
        { "token": "--uix-segment-on-bg", "default": "var(--uix-bg-raised)" }
      ],
      "status": "stable",
      "useFor": "switching between a few views of the same data",
      "insteadWhen": "the options are separate content sections (Tabs)",
      "accessibility": "`role=\"group\"` with `aria-label`; each option is a native button with `aria-pressed`, so state reaches assistive technology without a hidden radio hack"
    },
    {
      "component": "Select",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node: a field label can carry a hint or a required marker."
        },
        {
          "name": "options",
          "type": "SelectOption[]",
          "required": false,
          "doc": "The convenience form."
        },
        { "name": "hint", "type": "ReactNode", "required": false },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and for assistive technology."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["Select.Option", "Select.Group"],
      "slots": [],
      "status": "stable",
      "useFor": "one value from a short fixed list",
      "insteadWhen": "someone would rather type than scan (`Combobox`), or when the popup itself has to be yours — grouped rows, a second line per option, a checkmark, anything drawn",
      "accessibility": "a native `select`, so the picker, the typeahead, the keyboard and the form participation are the platform's. The chevron is `aria-hidden` decoration over the real control rather than a replacement for it, and the hint is linked with `aria-describedby` rather than left floating near the field"
    },
    {
      "component": "Skeleton",
      "props": [
        {
          "name": "shape",
          "type": "\"line\" | \"circle\" | \"block\"",
          "required": false,
          "doc": "Text-line placeholder by default; circle for avatars."
        },
        { "name": "width", "type": "string", "required": false },
        { "name": "height", "type": "string", "required": false }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "holding the shape of content that is about to arrive",
      "insteadWhen": "the wait has no known shape (Spinner)",
      "accessibility": "`aria-hidden` — the loading state belongs to the region that is loading, not to a decoration; pair it with a visually hidden or announced status where the wait matters"
    },
    {
      "component": "Slider",
      "props": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name."
        },
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, now that the accessible name comes from a real `<label>`."
        },
        { "name": "hint", "type": "ReactNode", "required": false },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and for assistive technology."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        { "name": "min", "type": "number", "required": false },
        { "name": "max", "type": "number", "required": false },
        { "name": "step", "type": "number", "required": false },
        { "name": "defaultValue", "type": "number", "required": false },
        { "name": "value", "type": "number", "required": false },
        {
          "name": "onValueChange",
          "type": "(value: number) => void",
          "required": false
        },
        {
          "name": "showValue",
          "type": "boolean",
          "required": false,
          "doc": "Show the live value next to the label."
        },
        {
          "name": "disabled",
          "type": "boolean",
          "required": false,
          "doc": "Declared here rather than inherited from the div, because `rest` lands on the wrapper and a `disabled` that never reaches the input is a control that looks unavailable and still moves."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a value where the approximate position matters more than the number",
      "insteadWhen": "the exact number matters (NumberField)",
      "accessibility": "`input[type=range]` is a fully accessible slider — arrows, PageUp/Down, Home/End and screen reader announcements come from the platform. `accent-color` themes it with one token. Note: a Base UI slider was evaluated and deferred (rc error #62 in test environments); adoption revisits at 1.0"
    },
    {
      "component": "Spinner",
      "props": [
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "Announced via role=\"status\"; changes re-announce politely."
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "a short wait with no known duration",
      "insteadWhen": "the layout is already known (Skeleton), or progress is measurable (ProgressBar)",
      "accessibility": "`role=\"status\"` with a visually hidden label, so screen readers announce the wait without seeing a shape"
    },
    {
      "component": "SplitButton",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "The default action's label."
        },
        {
          "name": "onAction",
          "type": "() => void",
          "required": false,
          "doc": "What the primary half does."
        },
        {
          "name": "items",
          "type": "MenuItemDescriptor[]",
          "required": false,
          "doc": "The alternatives, behind the arrow."
        },
        { "name": "children", "type": "ReactNode", "required": false },
        {
          "name": "menuLabel",
          "type": "string",
          "required": true,
          "doc": "What the arrow half is called, for assistive technology."
        },
        {
          "name": "variant",
          "type": "\"solid\" | \"outline\"",
          "required": false
        },
        {
          "name": "tone",
          "type": "\"accent\" | \"neutral\"",
          "required": false
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false
        },
        { "name": "disabled", "type": "boolean", "required": false },
        { "name": "loading", "type": "boolean", "required": false }
      ],
      "parts": [
        "SplitButton.Item",
        "SplitButton.Separator",
        "SplitButton.Group"
      ],
      "slots": [],
      "status": "beta",
      "useFor": "one action people take most of the time, with variants behind it — Save, with Save and close, Save as draft",
      "insteadWhen": "no option is the obvious default: that is a `Menu`, and pretending one of a set of equals is the default makes the others hard to find",
      "accessibility": "two buttons, not one button with two click regions. The primary half is a plain `<button>`; the arrow is a menu trigger with `aria-haspopup=\"menu\"` and its own accessible name from `menuLabel`. A single button that opened a menu when clicked on the right would be one control announcing one name and doing two things, and a keyboard could only ever reach one of them. The arrow is `aria-hidden`, beca"
    },
    {
      "component": "StatusPill",
      "props": [
        {
          "name": "renderAs",
          "type": "Renderable",
          "required": false,
          "doc": "Render as a different element, keeping every style and behaviour."
        },
        { "name": "tone", "type": "Tone", "required": true },
        {
          "name": "children",
          "type": "ReactNode",
          "required": true,
          "doc": "The state in words."
        }
      ],
      "parts": [],
      "slots": [
        { "token": "--uix-pill-fg", "default": "var(--uix-text-primary)" },
        { "token": "--uix-pill-off", "default": "var(--uix-status-off)" },
        { "token": "--uix-pill-ok", "default": "var(--uix-status-ok)" },
        { "token": "--uix-pill-warn", "default": "var(--uix-status-warn)" }
      ],
      "status": "stable",
      "useFor": "a live state that changes on its own",
      "insteadWhen": "the label is static (Badge)",
      "accessibility": "the dot is `aria-hidden` and the words carry the state, so the pill means the same thing to someone who cannot distinguish the colours. This is the component that exists to make that rule concrete: a coloured dot on its own is a state only some readers can read"
    },
    {
      "component": "Stepper",
      "props": [
        { "name": "steps", "type": "StepperStep[]", "required": true },
        {
          "name": "current",
          "type": "number",
          "required": true,
          "doc": "Which step the person is on, by index."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What the sequence is called."
        },
        {
          "name": "onStepChange",
          "type": "(index: number, step: StepperStep) => void",
          "required": false,
          "doc": "Make completed steps navigable."
        },
        {
          "name": "orientation",
          "type": "\"horizontal\" | \"vertical\"",
          "required": false,
          "doc": "Lay it out down the page rather than across it."
        },
        {
          "name": "errorAt",
          "type": "number",
          "required": false,
          "doc": "A step that failed, by index."
        },
        {
          "name": "marker",
          "type": "(entry: StepperStep, state: StepperState) => ReactNode",
          "required": false,
          "doc": "Render the circle yourself."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a task split into a fixed sequence where the reader needs to know how far along they are and what is left",
      "insteadWhen": "the sections can be done in any order — that is `Tabs`, and numbering them implies a dependency that does not exist",
      "accessibility": "an ordered list inside a `nav` named by `label`, and the current step carries `aria-current=\"step\"`. That combination is what lets a screen reader answer \"where am I\" — the visual position in a row of circles answers it for nobody else. The state of each step is in text as well as in colour, in a visually hidden span: \"completed\", \"current\", \"failed\". A stepper that says where you are only by fill"
    },
    {
      "component": "Switch",
      "props": [
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name, so a `Form` can route an error here by name."
        },
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node."
        },
        { "name": "checked", "type": "boolean", "required": false },
        { "name": "defaultChecked", "type": "boolean", "required": false },
        {
          "name": "onCheckedChange",
          "type": "(checked: boolean) => void",
          "required": false,
          "doc": "The checked triple, matching value/defaultValue/onValueChange elsewhere: checked, defaultChecked, onCheckedChange."
        },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "hint",
          "type": "ReactNode",
          "required": false,
          "doc": "Instruction under the control, linked with aria-describedby."
        },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the control required, visibly and for assistive technology."
        }
      ],
      "parts": [],
      "slots": [
        { "token": "--uix-switch-thumb-bg", "default": "var(--uix-bg-raised)" },
        { "token": "--uix-switch-track-bg", "default": "var(--uix-bg-subtle)" },
        { "token": "--uix-switch-track-on-bg", "default": "var(--uix-accent)" },
        { "token": "--uix-switch-travel", "default": "1.3rem" }
      ],
      "status": "stable",
      "useFor": "a setting that takes effect the moment it is flipped",
      "insteadWhen": "the choice is submitted with a form (Checkbox)",
      "accessibility": "`role=\"switch\"` with a literal `aria-checked`, named by `aria-labelledby` pointing at the visible label. The wrapping <label> makes the text clickable, but it does NOT name the control: implicit label association only works for real form elements, not for an ARIA role on a span, so without this the switch reaches assistive technology unnamed"
    },
    {
      "component": "Table",
      "props": [
        { "name": "caption", "type": "ReactNode", "required": false },
        { "name": "children", "type": "ReactNode", "required": true }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "rows compared across the same columns",
      "insteadWhen": "each row is an object to open (a list of Cards)",
      "accessibility": "markup stays yours — `thead`, `tbody` and `th scope` are what make a table navigable, and no wrapper can supply them for you. The caption is the table's accessible name and is visually hidden. The scroll container is focusable. `overflow-x: auto` creates a region only a pointer can reach, so a keyboard-only user could not scroll a wide table sideways at all — WCAG 2.1.1 in its plainest form. It ha"
    },
    {
      "component": "Tabs",
      "props": [
        {
          "name": "tabs",
          "type": "TabItem[]",
          "required": false,
          "doc": "The convenience form."
        },
        {
          "name": "defaultActiveIndex",
          "type": "number",
          "required": false,
          "doc": "Which tab starts selected, as an index into `tabs`."
        },
        { "name": "value", "type": "string", "required": false },
        { "name": "defaultValue", "type": "string", "required": false },
        {
          "name": "onValueChange",
          "type": "(value: string) => void",
          "required": false
        },
        {
          "name": "label",
          "type": "string",
          "required": false,
          "doc": "Accessible name for the tablist, e.g."
        },
        { "name": "children", "type": "ReactNode", "required": false }
      ],
      "parts": ["Tabs.List", "Tabs.Tab", "Tabs.Panel"],
      "slots": [],
      "status": "stable",
      "useFor": "peer sections of content in one context",
      "insteadWhen": "the sections are filters over one list (SegmentedControl)",
      "accessibility": "Base UI renders the tablist/tab/tabpanel wiring — `aria-selected`, `aria-controls`/`aria-labelledby`, roving tabindex — and handles Arrow/Home/End. Name the list with `label` in the shorthand form, or with `aria-label` on `Tabs.List` when composing. **Manual activation**, which is the half this used to leave out. The ARIA tabs pattern has two variants and \"per the ARIA pattern\" names neither, so a"
    },
    {
      "component": "TagInput",
      "props": [
        { "name": "label", "type": "ReactNode", "required": true },
        { "name": "hint", "type": "ReactNode", "required": false },
        { "name": "error", "type": "ReactNode", "required": false },
        { "name": "required", "type": "boolean", "required": false },
        { "name": "hideLabel", "type": "boolean", "required": false },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name, so a `Form` can route an error here."
        },
        { "name": "value", "type": "string[]", "required": false },
        { "name": "defaultValue", "type": "string[]", "required": false },
        {
          "name": "onValueChange",
          "type": "(next: string[]) => void",
          "required": false
        },
        { "name": "placeholder", "type": "string", "required": false },
        { "name": "disabled", "type": "boolean", "required": false },
        {
          "name": "normalise",
          "type": "(raw: string) => string | null",
          "required": false,
          "doc": "Reject or rewrite a tag before it is added."
        },
        {
          "name": "max",
          "type": "number",
          "required": false,
          "doc": "How many tags are allowed."
        },
        {
          "name": "tag",
          "type": "(value: string) => ReactNode",
          "required": false,
          "doc": "Render a tag's contents yourself."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a set of short free-text values the reader builds up — recipients, labels, filter terms",
      "insteadWhen": "the values come from a known list: that is a multi-select, and free text invites typos the list would have prevented",
      "accessibility": "the tags are a list, the input is a field, and the two are separate things a reader can walk. Each remove button carries the tag it removes in its accessible name — a column of buttons all called \"Remove\" is a column a screen reader reads as identical controls, which is the usual failing of this pattern. Removing a tag announces the change through a `role=\"status\"`, because pressing Backspace dele"
    },
    {
      "component": "TextField",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, so a label can carry a required marker or a hint link."
        },
        {
          "name": "hint",
          "type": "ReactNode",
          "required": false,
          "doc": "Supporting text under the field; wired via aria-describedby."
        },
        {
          "name": "error",
          "type": "ReactNode",
          "required": false,
          "doc": "Validation message."
        },
        {
          "name": "required",
          "type": "boolean",
          "required": false,
          "doc": "Marks the field required, visibly and for assistive technology."
        },
        {
          "name": "hideLabel",
          "type": "boolean",
          "required": false,
          "doc": "Render the label for assistive technology only."
        },
        {
          "name": "prefix",
          "type": "ReactNode",
          "required": false,
          "doc": "Slot before the input (e.g."
        },
        {
          "name": "suffix",
          "type": "ReactNode",
          "required": false,
          "doc": "Slot after the input (e.g."
        }
      ],
      "parts": [],
      "slots": [
        { "token": "--uix-field-pad-y", "default": "0" },
        {
          "token": "--uix-field-radius",
          "default": "var(--uix-radius-control)"
        }
      ],
      "status": "stable",
      "useFor": "any single-line text entry",
      "insteadWhen": "the field queries a collection (SearchInput)",
      "accessibility": "`label` is bound with `for`; hint and error are linked through `aria-describedby`; an error also sets `aria-invalid`, so the state reaches assistive technology without relying on the red border"
    },
    {
      "component": "Textarea",
      "props": [
        {
          "name": "label",
          "type": "ReactNode",
          "required": true,
          "doc": "A node, so a label can carry a required marker or a hint link."
        },
        { "name": "hint", "type": "ReactNode", "required": false },
        { "name": "error", "type": "ReactNode", "required": false },
        { "name": "required", "type": "boolean", "required": false },
        { "name": "hideLabel", "type": "boolean", "required": false },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "doc": "The form field name, so a `Form` can route an error here."
        },
        {
          "name": "autoGrow",
          "type": "boolean",
          "required": false,
          "doc": "Grow with the text instead of scrolling inside a fixed box."
        },
        {
          "name": "maxRows",
          "type": "number",
          "required": false,
          "doc": "How tall it may grow before it scrolls."
        },
        {
          "name": "showCount",
          "type": "boolean",
          "required": false,
          "doc": "Show how much is left, and announce it as it runs out."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "text longer than a line — a note, a message, a description",
      "insteadWhen": "it is one line (`TextField`), or the content has structure the reader expects to see rendered, which is an editor and not a form field",
      "accessibility": "the same wiring as every other field, through `Field` — the label with `for`, hint and error through `aria-describedby`, `aria-invalid` on an error. The three things a textarea adds on top: `resize` is left to the browser. A `resize: none` is the most common line in a textarea's stylesheet and it takes away the one control the platform gives someone whose text does not fit — which is a WCAG 1.4.4 "
    },
    {
      "component": "Toaster",
      "props": [
        { "name": "toasts", "type": "ToastItem[]", "required": true },
        {
          "name": "onDismiss",
          "type": "(id: string) => void",
          "required": true
        },
        {
          "name": "position",
          "type": "\"bottom-right\" | \"top-center\"",
          "required": false,
          "doc": "Position of the stack."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "confirming something that already happened, without interrupting",
      "insteadWhen": "the reader must act on it (Dialog or Alert)",
      "accessibility": "one region per severity role — success/info announce politely, warning/danger assertively. The stack is a labelled region, so screen reader users can revisit it"
    },
    {
      "component": "Toolbar",
      "props": [
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What this set of controls is for."
        },
        { "name": "children", "type": "ReactNode", "required": true },
        {
          "name": "orientation",
          "type": "\"horizontal\" | \"vertical\"",
          "required": false
        }
      ],
      "parts": ["Toolbar.Separator", "Toolbar.Group"],
      "slots": [],
      "status": "beta",
      "useFor": "a row of controls that act on the same thing — the actions above a table, the formatting controls over an editor",
      "insteadWhen": "the controls are unrelated: a `div` with a gap is the right amount of structure for two buttons that happen to sit near each other, and a toolbar makes a promise about the keyboard that then has to be kept",
      "accessibility": "`role=\"toolbar\"` with a name, and **one tab stop for the whole group**. That second part is the entire reason to reach for this instead of a `div`. Eight buttons in a row are eight tab stops on the way to the content below them; a toolbar is one, and the arrow keys move between the controls inside it. That is what WAI-ARIA's toolbar pattern is for, and it is the thing a styled `div` cannot give yo"
    },
    {
      "component": "Tooltip",
      "props": [
        {
          "name": "content",
          "type": "ReactNode",
          "required": true,
          "doc": "A node: a tooltip often needs a keyboard hint or emphasis."
        },
        {
          "name": "children",
          "type": "ReactElement",
          "required": true,
          "doc": "The focusable trigger."
        },
        {
          "name": "placement",
          "type": "\"top\" | \"bottom\"",
          "required": false
        },
        {
          "name": "open",
          "type": "boolean",
          "required": false,
          "doc": "Controlled: a tooltip driven by something other than the pointer."
        },
        { "name": "defaultOpen", "type": "boolean", "required": false },
        {
          "name": "onOpenChange",
          "type": "(open: boolean) => void",
          "required": false
        }
      ],
      "parts": [],
      "slots": [],
      "status": "stable",
      "useFor": "naming an icon-only control, or a short hint",
      "insteadWhen": "the content is essential or interactive (put it in the page)",
      "accessibility": "the announcement is **ours**, not Base UI's, and the distinction is the whole reason this paragraph got rewritten. This used to say Base UI announced the hint \"(its own wiring)\". Measured against `1.0.0-rc.0` in the open state, the trigger's attributes were `type,class,data-variant,data-tone,data-size,id,data-popup-open` — no `aria-describedby` — and the popup carried no role. A tooltip with neith"
    },
    {
      "component": "Tree",
      "props": [
        { "name": "nodes", "type": "TreeNode[]", "required": true },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "doc": "What the tree is."
        },
        { "name": "expanded", "type": "string[]", "required": false },
        { "name": "defaultExpanded", "type": "string[]", "required": false },
        {
          "name": "onExpandedChange",
          "type": "(next: string[]) => void",
          "required": false
        },
        { "name": "selected", "type": "string | null", "required": false },
        {
          "name": "defaultSelected",
          "type": "string | null",
          "required": false
        },
        {
          "name": "onSelectedChange",
          "type": "(next: string) => void",
          "required": false
        },
        {
          "name": "node",
          "type": "(entry: TreeNode, depth: number) => ReactNode",
          "required": false,
          "doc": "Render a node's label yourself."
        }
      ],
      "parts": [],
      "slots": [],
      "status": "beta",
      "useFor": "a hierarchy the reader navigates — folders, an organisation, a category system",
      "insteadWhen": "the nesting is one level deep: that is a list with headings, and a tree makes a promise about the keyboard that then has to be kept",
      "accessibility": "`role=\"tree\"` with `treeitem` children, and **one tab stop for the whole tree**. Arrows do the work: down and up move through the visible items, right opens a closed branch or steps into an open one, left closes an open branch or steps out to the parent, Home and End go to the ends. That is the WAI-ARIA tree pattern, and it is the reason to reach for this instead of nested lists — a hundred-node t"
    }
  ]
}
